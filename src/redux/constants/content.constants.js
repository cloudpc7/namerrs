/**
 * content.constants.js — Redux action types and slice name for content state.
 */

export const CONTENT_SLICE_NAME = 'content';

export const CONTENT_STATUS = {
  IDLE: 'idle',
  LOADING: 'loading',
  SUCCEEDED: 'succeeded',
  FAILED: 'failed',
};